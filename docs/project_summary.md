# ????

## ????

- **?????`install/index.php`?**?
  - ???????PHP ???`mysqli` ?????????
  - ?????????????
  - ????????????`admins`?`goods`?`service_agents`?`service_messages`??
  - ?????????????? `config/config.php` ??? `install/install.lock`?
- **???**?
  - `index.php` ????????????????????????????
  - `detail.php` ???????????????
  - `service.php` ????????????????????????????
- **?????**?
  - `admin/login.php` ????????
  - `admin/goods_add.php` ???????????? `uploads/`?
  - `admin/goods_list.php` ?????????????????????? CSRF token ???
  - `admin/goods_edit.php` ????????????????
- **????**?
  - `service/login.php`/`service/logout.php` ??????????
  - `service/message.php` ??????????????????????????????????????

## ??????

```
77:154:install/index.php
function createDatabase(array $dbConfig, array &$errors, array &$messages): bool
{
    $conn = @new mysqli(
        $dbConfig['host'],
        $dbConfig['user'],
        $dbConfig['pass'],
        '',
        (int)$dbConfig['port']
    );
    // ... existing code ...
}
```

```
1:120:service.php
if (isset($_GET['ajax']) && $_GET['ajax'] === '1') {
    header('Content-Type: application/json');
    $nickname = trim($_GET['nickname'] ?? '');
    // ... existing code ...
}
```

```
1:130:admin/goods_list.php
if (isset($_GET['delete'], $_GET['token'])) {
    if (hash_equals($token, $_GET['token'])) {
        $id = (int)$_GET['delete'];
        // ... existing code ...
    }
}
```

```
9:162:service/message.php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $selectedId = (int)($_POST['message_id'] ?? 0);
    // ... existing code ...
}
```

```
1:44:database.sql
CREATE TABLE IF NOT EXISTS `admins` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL UNIQUE,
  `password` varchar(255) NOT NULL,
  `email` varchar(100) NOT NULL,
  `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);
```

## ????

1. ?? PHP ???????? `php -S 0.0.0.0:8000 -t /workspace`?
2. ?? `http://localhost:8000/install/index.php` ???????
3. ??????????????????????????????
4. ?????????????????????????????????

## ????

- ?? `config/`?`uploads/`?`install/` ?????????
- ??????? `install/install.lock` ??? `install/` ?????????
- ????????? `config/config.php` ???????????????
